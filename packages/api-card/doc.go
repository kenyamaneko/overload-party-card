// Package apicard exposes the wire types that overload-party-card publishes
// on its internal REST surface. Gateway/battle consume this package to stay in
// sync with card's internal contract without reaching into card's private
// packages.
package apicard
